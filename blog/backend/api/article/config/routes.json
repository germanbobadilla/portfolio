{
  "routes": [
    {
      "method": "GET",
      "path": "/article",
      "handler": "article.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/article",
      "handler": "article.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/article",
      "handler": "article.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
